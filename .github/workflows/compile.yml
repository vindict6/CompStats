name: Compile and Release CompStats

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Plugin
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bump Version and Create Tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch

      - name: Setup .NET 8.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore Dependencies
        run: dotnet restore CompStats.csproj

      - name: Build and Publish
        run: dotnet publish CompStats.csproj -c Release -o ./publish_temp

      - name: Prepare Folder Structure
        run: |
          mkdir -p ./release_stage/addons/counterstrikesharp/plugins/CompStats
          mv ./publish_temp/CompStats.dll ./release_stage/addons/counterstrikesharp/plugins/CompStats/
          mv ./publish_temp/CompStats.pdb ./release_stage/addons/counterstrikesharp/plugins/CompStats/
          mv ./publish_temp/CompStats*.json ./release_stage/addons/counterstrikesharp/plugins/CompStats/ 2>/dev/null || true

      - name: Zip Release Artifact
        run: |
          cd release_stage
          zip -r "CompStats-${{ steps.tag_version.outputs.new_tag }}.zip" addons/

      - name: Upload Build Artifact (Actions Tab)
        uses: actions/upload-artifact@v4
        with:
          name: CompStats-${{ steps.tag_version.outputs.new_tag }}
          path: release_stage/CompStats-${{ steps.tag_version.outputs.new_tag }}.zip
          retention-days: 7

      - name: Publish to GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag_version.outputs.new_tag }}
          files: release_stage/CompStats-${{ steps.tag_version.outputs.new_tag }}.zip
          generate_release_notes: true
